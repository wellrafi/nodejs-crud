<!-- DataTales Example -->
<div class="card mb-4">
	<div class="card-header py-3">
		<!-- Button trigger modal -->
		<button type="button" class="btn btn-primary" data-toggle="modal" data-target="#add-modal-jenis-peleko">
			<i class="fas fa-plus"></i>
			<span class="ml-1">Tambah jenis peleko</span>
		</button>
	</div>
	<div class="card-body">
		<div class="table-responsive">
			<table class="table" id="dataTable" width="100%" cellspacing="0">
				<thead>
					<tr>
						<th style="width: 50px;">No</th>
						<th>Nama</th>
						<th style="width: 100px;">Action</th>
					</tr>
				</thead>
				<tbody>
					<% for( let i = 0; i < jenisPeleko.length; i++ ) { %>
					<tr>
						<td><%= i + 1 %></td>
						<td><%= jenisPeleko[i].nama %></td>
						<td>
							<form action="/admin/jenisPeleko/<%= jenisPeleko[i].id %>?_method=DELETE" method="POST">
								<button
									type="button"
									class="btn btn-icon btn-warning btn-circle btn-sm btn-update-jenis-peleko"
									data-toggle="modal"
									data-target="#edit-modal-jenis-peleko"
									data-id="<%= jenisPeleko[i].id %>"
									data-name="<%= jenisPeleko[i].nama %>"
								>
									<i class="fas fa-edit"></i>
								</button>
								<button
									type="button"
									data-toggle="modal"
									data-target="#delete-modal-jenis-peleko"
									data-id="<%= jenisPeleko[i].id %>"
									class="btn btn-icon btn-danger btn-circle btn-sm btn-delete-jenis-peleko"
								>
									<i class="fas fa-trash"></i>
								</button>
							</form>
						</td>
					</tr>
					<% } %>
				</tbody>
			</table>
		</div>
	</div>
</div>
